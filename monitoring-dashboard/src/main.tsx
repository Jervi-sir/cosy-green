import React from "react";
import ReactDOM from "react-dom/client";

import { MonitoringDashboardPage } from "./monitoring-dashboard-page";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MonitoringDashboardPage />
  </React.StrictMode>,
);
